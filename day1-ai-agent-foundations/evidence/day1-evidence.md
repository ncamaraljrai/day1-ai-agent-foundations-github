# Day 1 — Real Execution Evidence

**Model:** `qwen2.5:7b`  
**Ollama host:** `http://localhost:11434`  
**Evidence generated:** 2026-09-02  

> These traces were generated by a real local model call through Ollama. They are not mocked or replayed.

## Lab 1.2 — Normal run 1

- **Steps:** 4
- **Tool order:** lookup_order → get_today → count_business_days
- **Stopped by:** model_final
- **Input tokens:** 2302
- **Output tokens:** 538
- **Total tokens:** 2840
- **Observed behavior:** Used 3 tool call(s): lookup_order → get_today → count_business_days.

## Lab 1.2 — Normal run 2

- **Steps:** 4
- **Tool order:** lookup_order → get_today → count_business_days
- **Stopped by:** model_final
- **Input tokens:** 2318
- **Output tokens:** 434
- **Total tokens:** 2752
- **Observed behavior:** Used 3 tool call(s): lookup_order → get_today → count_business_days.

### Non-determinism comparison

- Run 1 path: `lookup_order → get_today → count_business_days`
- Run 2 path: `lookup_order → get_today → count_business_days`
- Same path: **YES**

## Dependency evidence

`count_business_days(start, end)` cannot know its concrete arguments until earlier information is available:
- `start` comes from `get_today()`;
- `end` is derived from `lookup_order()` invoice date + terms.

The stronger agentic dependency is behavioral: if `lookup_order()` returns an error, the appropriate next action changes instead of blindly continuing the happy path.

## Who decides to stop?

The model expresses the semantic stop decision through `response.stop_reason`. The orchestration code reads it at:

```python
if response.stop_reason != "tool_use":
```

The hard `MAX_STEPS` ceiling is independently owned by the program.

## Modification A — weak `get_today` description

- **Steps:** 5
- **Tool order:** lookup_order → lookup_order → get_today → count_business_days
- **Input tokens:** 3075
- **Output tokens:** 527
- **Observed behavior:** Used 4 tool call(s): lookup_order → lookup_order → get_today → count_business_days.

## Modification B — order #9999

- **Steps:** 2
- **Tool order:** lookup_order
- **Stopped by:** model_final
- **Input tokens:** 875
- **Output tokens:** 195
- **Observed behavior:** Used 1 tool call(s): lookup_order.
- **Final answer:** It seems there is no order with the ID '9999'. The known order IDs are '4471' and '4472'. Let's proceed with one of these IDs to determine the number of business days left before the invoice goes overdue.

Shall we use order ID '4471' for this calculation? If not, please provide the correct order ID.

## Modification C — `MAX_STEPS = 2`

- **Steps:** 2
- **Tool order:** lookup_order → count_business_days
- **Stopped by:** max_steps
- **Input tokens:** 900
- **Output tokens:** 255
- **Observed behavior:** Hit the hard step ceiling before the model returned a final answer.

A production agent still needs this limit because a model can wander, repeat calls, or continue searching indefinitely. The ceiling bounds cost and latency even when it occasionally truncates a valid path.

## Lab 1.3 — Plain call

- **Stop reason:** end_turn
- **Input tokens:** 55
- **Output tokens:** 198
- **Total tokens:** 253

### Plain-call output

> To determine how many business days are left before the invoice for order #4471 goes overdue, I would need the following information:
> 
> 1. The current date.
> 2. The due date of the invoice for order #4471.
> 3. Whether weekends and holidays are considered non-business days.
> 
> Without this information, I can't provide a specific number of business days. However, if you provide these details, I can calculate the number of business days remaining. 
> 
> For example, if the current date is October 5, 2023, and the due date is October 10, 2023, and assuming no holidays, the remaining business days would be 5 (October 6, 7, 9, 10). If a weekend (October 7 and 8) is considered a non-business day, then the remaining business days would be 3 (October 6, 9, 10).

## Agent versus plain-call token comparison

| Run | Input | Output | Total |
|---|---:|---:|---:|
| Plain call | 55 | 198 | 253 |
| Agent run 1 | 2302 | 538 | 2840 |

The agent's additional spend buys tool access, iterative observation, and the possibility of recovery from missing or changing information.

## Temporal reproducibility run

The supplied fixture has order `4471` invoiced on `2026-03-03` with 30-day terms, while the script uses the machine's real current date. Because that scenario becomes stale after April 2026, I also ran a **separately labelled reproduction** with `get_today()` frozen to `2026-03-24`.

- **Frozen date:** 2026-03-24
- **Steps:** 3
- **Tool order:** lookup_order → count_business_days
- **Input tokens:** 1526
- **Output tokens:** 316
- **Final answer:** Based on the information provided, the due date for the invoice of order #4471 is April 2, 2026. From today's date, there are 21 business days left before the invoice goes overdue. 

This calculation takes into account that weekends and public holidays are not considered working days.

This frozen-date run is for reproducing the teaching scenario only; it is not represented as the current date.

## Raw trace files

See `evidence/raw/` for complete per-step action/result traces.
